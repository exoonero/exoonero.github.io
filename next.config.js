/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        appDir: true
    },
    output: 'export',
    basePath: 'src/app/assets/images',
}

module.exports = nextConfig
